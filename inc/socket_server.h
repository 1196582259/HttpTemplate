#pragma once
#include<iostream>
#include <string.h>
class HttpServer
{
private:
    /* data */
    std::string ipv4;
    
public:
    HttpServer(/* args */);
    ~HttpServer();
};

HttpServer::HttpServer(/* args */)
{
}

HttpServer::~HttpServer()
{
}
